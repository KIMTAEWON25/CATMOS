import pandas as pd 
from sklearn.cluster import DBSCAN
import matplotlib.pyplot  as plt
import seaborn as sb
import numpy as np
from mpl_toolkits.mplot3d import Axes3D
from sklearn.cluster import KMeans
from sklearn.neighbors import KNeighborsRegressor 
import csv

a_list = []
f = open('./data_1.csv', 'r')
reader = csv.reader(f)
for row in reader:
    a_list.append(row)
f.close()  


sum=0

def euclidean_distance(pt1, pt2):
  distance = 0
  for i in range(0,len(pt1)):
    distance += int((int(pt1[i]) - int(pt2[i])) ** 2)
  return distance ** 0.5

for i in range(1,len(a_list)-1):
    print(int(euclidean_distance(a_list[i], a_list[i+1])))
    sum=sum+int(euclidean_distance(a_list[i], a_list[i+1]))

#for i in range(0,len(a_list)):
#    print(list(map(int, a_list[i])))
    
print(sum)

    
      
#print(euclidean_distance(a_list[1], a_list[2]))
